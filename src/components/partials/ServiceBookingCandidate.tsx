import { Service } from "@/interfaces";

interface ServiceBookingCandidateProps {
  service: Service;
}

const ServiceBookingCandidate = ({ service }: ServiceBookingCandidateProps) => {
  return <div>ServiceBookingCandidate</div>;
};

export default ServiceBookingCandidate;
